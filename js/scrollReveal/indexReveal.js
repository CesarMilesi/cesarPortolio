ScrollReveal({
    reset: true,
    distance: '60px',
    duration: 2500,
    delay: 400
});

ScrollReveal().reveal('.title', { cleanup: true, delay: 500, origin: 'left' });
ScrollReveal().reveal('h3', { cleanup: true, delay: 500, origin: 'right' });
ScrollReveal().reveal('.text', { cleanup: true, delay: 500, origin: 'left' });
ScrollReveal().reveal('.btn-box', { cleanup: true, delay: 500, origin: 'top' });
ScrollReveal().reveal('.social-mediaBis', { cleanup: true, delay: 500, origin: 'bottom' });
ScrollReveal().reveal('.image', { cleanup: true, delay: 500, origin: 'right' });
ScrollReveal().reveal('.about-Img', { cleanup: true, delay: 500, origin: 'left' });

// ScrollReveal().clean('.title');
// ScrollReveal().clean('h3');
// ScrollReveal().clean('.text');
// ScrollReveal().clean('.btn-box');
// ScrollReveal().clean('.social-mediaBis');
// ScrollReveal().clean('.image');
// ScrollReveal().clean('.about-Img');